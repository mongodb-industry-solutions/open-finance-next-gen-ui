FROM node:22-alpine
WORKDIR /app
COPY frontend/package*.json ./
RUN npm install
COPY frontend/ .
RUN npm run build
# Kanopy requires port 8080
ENV PORT=8080
EXPOSE 8080
# Start Next.js on port 8080
CMD ["npm", "start", "--", "-p", "8080"]
