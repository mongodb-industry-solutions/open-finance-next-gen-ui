services:
  openfinance-nextgen-ui-frontend:
    build:
      context: .
      no_cache: true
      dockerfile: Dockerfile.frontend
    image: openfinance-nextgen-ui-frontend:latest
    ports:
      - "3000:3000"
    restart: always
    container_name: openfinance-nextgen-ui-frontend